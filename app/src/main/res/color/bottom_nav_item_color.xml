<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Color for active (selected) tab -->
    <item android:color="@color/active_tab_color" android:state_checked="true" />

    <!-- Color for inactive (non-selected) tab -->
    <item android:color="@color/inactive_tab_color" android:state_checked="false" />
</selector>